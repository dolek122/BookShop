@model IEnumerable<Genre>
@{
    Layout = "_AdminLayout";
}

<h2>Gatunki</h2>

<a asp-action="AddGenre" asp-controller="Genre" class="btn btn-primary">Dodaj więcej</a>

<table class="table table-striped my-2">
    <tr>
        <th>Gatunek</th>
        <th>Akcja</th>
    </tr>

    @foreach(var genre in Model)
    {
        <tr>
            <td>@genre.GenreName</td>
            <td>
                <a asp-action="UpdateGenre" asp-controller="Genre" asp-route-id="@genre.Id" class="btn btn-success">Edytuj</a>
                <a asp-action="DeleteGenre" asp-controller="Genre" asp-route-id="@genre.Id" class="btn btn-danger" onclick="return window.confirm('Jesteś pewny?')">Usuń</a>
            </td>
        </tr>
    }
</table>

